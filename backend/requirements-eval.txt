# Install in .venv-eval, not the backend runtime environment.
# DeepEval requires posthog>=7 while ChromaDB currently requires posthog<6.
deepeval==4.1.3
ragas==0.4.3

# Ragas 0.4.3 still imports legacy langchain-community chat model modules
# removed in the 0.4.x line. Keep this compatible set until Ragas fixes it.
langchain==0.3.27
langchain-community==0.3.31
langchain-core==0.3.79
langchain-openai==0.3.35
